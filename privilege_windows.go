//go:build windows

package main

// У Windows нет модели uid/gid/setuid POSIX-систем — привилегии там
// устроены совсем иначе (токены доступа, а не смена идентификатора
// процесса). Служба Windows обычно с самого начала настраивается на
// запуск под нужной учётной записью (при создании службы), а не стартует
// от администратора и потом сама себя понижает — поэтому сама идея
// "забиндить порт от admin и сброситься" сюда не переносится напрямую.
//
// dropPrivileges здесь — намеренная заглушка, а не реализация: реальный
// сброс привилегий под Windows не сделан и требует отдельной работы
// (скорее всего через настройку самой службы, а не через код резолвера).
func dropPrivileges(username string) error {
	return nil
}

// isElevated: проверка "запущено ли от администратора" на Windows требует
// другого API (проверка токена процесса), а не простого syscall — не
// реализовано, возвращаем false, чтобы не врать через простую заглушку.
func isElevated() bool {
	return false
}
